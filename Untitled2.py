import turtle
wn= turtle.Screen()

for i in range(6):
    for i in range(4):
        turtle.forward(20)
        turtle.right(90)
        turtle.penup()
        turtle.right(45)
        turtle.forward(14)
        turtle.left(45)
        turtle.pendown()
        length += 20
        